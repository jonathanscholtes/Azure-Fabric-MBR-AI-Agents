# Template Thumbnails

Slide thumbnails are generated automatically by the MCP server each time `fill_mbr_template` runs and are stored in Blob Storage under `thumbnails/decks/{deck_id}/slide-NN.png`.

No manual generation or upload is needed.
